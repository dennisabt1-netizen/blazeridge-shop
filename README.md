# BlazeRidge Shop (static)

Dark + ember product landing page. Buy buttons → Stripe Payment Links.

## Local preview
Open `index.html` or: `python3 -m http.server 8080` in this folder.

## Deploy
Any static host: GitHub Pages, Cloudflare Pages, Netlify — publish this folder as the site root.
